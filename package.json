{
  "name": "pedisettivenkatesh.github.io",
  "version": "1.0.0",
  "description": "Portifolio of Pedisetti Venkatesh",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "build": "npm run clean && parcel build index.html --no-cache --dist-dir dist"
  },
  "author": "Pedisetti Venkatesh",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.12.0",
    "rimraf": "^5.0.0",
    "@parcel/transformer-html": "^2.12.0"
  }
}
